# Phase 0 — bộ tối giản. Version đã kiểm tra trên PyPI + build thật, không phỏng đoán.
duckdb==1.5.5
boto3==1.43.78
pandas==3.0.5
pyarrow==25.0.1
matplotlib==3.11.1

# Cố tình KHÔNG cài s3fs: nó ghim botocore rất chặt (qua aiobotocore) và xung khắc
# với boto3 mới. Phase 0 không cần nó — boto3 lo upload, DuckDB tự đọc s3://.

# Phase 2 — Delta Lake. Dùng delta-rs (Rust, không cần JVM) thay vì delta-spark:
# Spark là nội dung Phase 3, chưa muốn kéo nó vào đây. delta-rs đọc/ghi ĐÚNG
# chuẩn Delta protocol nên _delta_log sinh ra y hệt Databricks thật.
deltalake==1.6.3

# Phase 3 — Spark. CHÚ Ý: đây là `pyspark-client`, KHÔNG phải `pyspark`.
# `pyspark` đầy đủ kéo theo cả bản phân phối Spark + JVM (~400MB) vào image
# notebook — thứ hoàn toàn thừa, vì tính toán diễn ra ở container spark-connect.
# `pyspark-client` (mới có từ Spark 4) chỉ gồm DataFrame API + gRPC: một client
# mỏng đúng nghĩa. Đây chính là mô hình Databricks Connect.
# Version phải khớp server (apache/spark:4.1.3).
pyspark-client==4.1.3

# Phase 5 — client Trino thuần Python (giao thức HTTP, không JVM, không gRPC).
# Cùng version đã kiểm trên PyPI dùng cho image Superset.
trino==0.339.0
